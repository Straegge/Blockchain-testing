package com.the_pangaea_paradigm.utilities;

/**
 * Class with variables instead of Enum, because "Attribute value must be constant" in Java Annotations.
 */
public final class Routes {
    public static final String PROJECT_PAGE_ROUTE = "projects";
}
